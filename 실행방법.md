LangFuse 웹사이트 가동 (로그인 창 안 보이고, To App 버튼 클릭)
Dockerdesktop 띄우기: PGVECTOR 플레이버튼 클릭
powershell에서 가상환경 진입(.\.venv\Scripts\activate) 후 Backend 서버 띄우기: cd packages/rag-backend, python run_server.py (Applcation startup complete 뜰때까지 대기)
powershell에서 가상환경 진입 후 Frontend 서버 띄우기: cd packages/rag-frontend && npm run dev (Local: http://localhost:5173/ 뜰때까지 대기 후, 5173 링크 클릭)
여기까지 진행하면 Frontend UI에서 Q&A 진행 가능함.
자동화 시험을 원하는 경우, 아래 단계 진행
Powershell에서 가상환경 진입 후, packages/rag-backend/tests 폴더로 이동
python evaluate.py --jsonl rag-evaluations.jsonl (평가 결과 출력)